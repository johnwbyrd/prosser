{
  "name": "prosser",
  "version": "1.0.0",
  "description": "AWS Bedrock OpenAI-compatible API proxy",
  "license": "GPL-3.0",
  "scripts": {
    "start": "sam local start-api",
    "test": "jest",
    "deploy": "bash scripts/deploy.sh"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.468.0",
    "@aws-sdk/client-dynamodb": "^3.468.0",
    "@aws-sdk/lib-dynamodb": "^3.468.0",
    "aws-xray-sdk": "^3.5.3"
  },
  "devDependencies": {
    "aws-sam-cli": "^1.94.0",
    "jest": "^29.7.0"
  }
} 